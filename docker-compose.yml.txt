version: '3'

services:

  spark:
    image: bitnami/spark:latest
    ports:
      - "8080:8080"

  minio:
    image: minio/minio

    ports:
      - "9000:9000"

    environment:
      MINIO_ROOT_USER: admin
      MINIO_ROOT_PASSWORD: password123

    command: server /data